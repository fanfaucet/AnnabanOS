[project]
name = "annaban-benchmark"
version = "0.1.0"
description = "Multi-model consensus benchmarking + governance layer"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "scikit-learn"
]

[project.scripts]
annaban = "cli:main"
